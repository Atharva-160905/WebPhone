/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your existing configuration can go here.
  // For now, a minimal config is best to ensure deployment works.
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
